import XCTest
@testable import TapSwiftFixesSPM

final class TapSwiftFixesSPMTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TapSwiftFixesSPM().text, "Hello, World!")
    }
}
